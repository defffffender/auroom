# Django and core
Django>=5.1,<6.0
python-dotenv>=1.0.0

# Database
psycopg2-binary>=2.9.9

# Production server
gunicorn>=21.0.0
gevent>=24.0.0
psycogreen>=1.0.2

# Caching
django-redis>=5.4.0

# Image processing
Pillow>=10.0.0

# PostgreSQL Full-Text Search (included in Django, but need contrib)
# No extra package needed - uses django.contrib.postgres

# Быстрый старт для деплоя AuRoom

## Что было сделано

1. ✅ Создан скрипт генерации 100,000 тестовых товаров
2. ✅ Добавлены индексы БД для оптимизации запросов
3. ✅ Проверена пагинация (12 товаров на страницу)
4. ✅ Созданы конфигурационные файлы для production
5. ✅ Создан автоматический скрипт деплоя

## Новые файлы

- `generate_test_products.py` - команда для генерации тестовых товаров
- `requirements.txt` - обновлен с PostgreSQL, Gunicorn, Redis
- `.env.production` - шаблон переменных окружения для production
- `gunicorn_config.py` - конфигурация Gunicorn сервера
- `nginx_auroom.conf` - конфигурация Nginx
- `auroom.service` - systemd сервис для автозапуска
- `deploy.sh` - автоматический скрипт деплоя
- `DEPLOYMENT.md` - полная инструкция по деплою

## Как задеплоить на VPS (3 простых шага)

### Шаг 1: Архивируйте проект

На вашем компьютере (в папке проекта):

```bash
# Создайте архив (без виртуального окружения)
tar -czf auroom.tar.gz --exclude='venv' --exclude='__pycache__' --exclude='*.pyc' --exclude='db.sqlite3' --exclude='media/*' --exclude='.git' .
```

Или используйте WinRAR/7-Zip для создания архива .tar.gz

### Шаг 2: Загрузите на VPS

Используйте любой способ:

**Вариант А: Через SCP (Git Bash или PowerShell)**
```bash
scp auroom.tar.gz root@185.217.131.179:/root/
```

**Вариант Б: Через FileZilla/WinSCP**
- Host: 185.217.131.179
- Port: 22
- Username: root
- Password: MR8Q8kEwH2ScWk8b
- Загрузите файл в /root/

### Шаг 3: Подключитесь к VPS и запустите деплой

```bash
# Подключение
ssh root@185.217.131.179
# Пароль: MR8Q8kEwH2ScWk8b

# Распаковка
cd /root
mkdir -p auroom
tar -xzf auroom.tar.gz -C auroom
cd auroom

# Запуск автоматического деплоя
bash deploy.sh
```

Скрипт автоматически:
- Установит PostgreSQL, Nginx, Redis
- Создаст базу данных
- Настроит виртуальное окружение Python
- Применит миграции
- Соберет статические файлы
- Запустит все сервисы

## Шаг 4: Генерация 100,000 товаров

После успешного деплоя:

```bash
cd /root/auroom
source venv/bin/activate
python manage.py generate_test_products --count 100000 --batch-size 1000
```

Это займет 15-30 минут. Вы увидите прогресс:

```
Создано: 50000/100000 (50.0%)
```

## Шаг 5: Проверка работы

Откройте в браузере:
- http://185.217.131.179
- http://vps6127.eskiz.uz

## Нагрузочное тестирование

```bash
# Установка Apache Bench
apt-get install apache2-utils -y

# Тест главной страницы (1000 запросов)
ab -n 1000 -c 10 http://185.217.131.179/

# Тест с фильтрами
ab -n 500 -c 10 "http://185.217.131.179/?category=kolca"

# Тест страницы товара
ab -n 500 -c 10 http://185.217.131.179/product/TEST-000001/
```

## Полезные команды

```bash
# Просмотр логов
journalctl -u auroom -f

# Перезапуск приложения
systemctl restart auroom

# Проверка статуса
systemctl status auroom
systemctl status nginx
systemctl status postgresql

# Количество товаров в БД
cd /root/auroom && source venv/bin/activate
python manage.py shell -c "from catalog.models import Product; print(Product.objects.count())"
```

## Если что-то пошло не так

1. Проверьте логи: `journalctl -u auroom -f`
2. Проверьте .env файл: `nano /root/auroom/.env`
3. Перезапустите сервисы: `systemctl restart auroom nginx`
4. Смотрите полную инструкцию в `DEPLOYMENT.md`

## Что дальше?

- Создайте суперпользователя: `python manage.py createsuperuser`
- Админ панель: http://185.217.131.179/admin
- Проведите нагрузочное тестирование
- Мониторьте производительность через `htop`

## Оптимизации, которые уже применены

1. ✅ Индексы БД для всех часто используемых полей
2. ✅ Пагинация (12 товаров на страницу)
3. ✅ select_related и prefetch_related для оптимизации запросов
4. ✅ PostgreSQL вместо SQLite (для production)
5. ✅ Gunicorn с несколькими воркерами
6. ✅ Nginx для статики и проксирования
7. ✅ Redis для кэширования (опционально)
8. ✅ Gzip сжатие в Nginx

Удачи с тестированием! 🚀
